spring:
  h2:
    console:
      enabled: true
  datasource:
    url: jdbc:h2:mem:testdb
    username: sa
    password: sa
  jpa:
    properties:
      hibernate:
        show_sql: true
  eureka:
    client:
      serviceUrl:
        defaultZone: http://localhost:8052/eureka